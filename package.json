{
  "name": "codenotch-win",
  "productName": "Codenotch",
  "version": "0.3.0",
  "description": "A Windows notch overlay that pins to the top of the screen and shows the live usage / credit of the AI coding accounts on this machine (DSH DeepSeek, OpenRouter, Claude, Codex, Antigravity).",
  "main": "src/main.js",
  "author": "ported for local use",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "electron .",
    "lint": "node scripts/check-syntax.js",
    "package": "node scripts/check-syntax.js && node scripts/package.js",
    "smoke": "node scripts/smoke-test.js",
    "test": "node --test tests/core.test.js"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-packager": "^17.1.2"
  }
}
